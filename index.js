import React from "react";
import ReactDOM from "react-dom/client";
import CotacoesApp from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <CotacoesApp />
  </React.StrictMode>
);